<!DOCTYPE HTML>
<html>

<head>
  <title>我的阅读周记</title>
</head>

<body>

<br>

<% for (var tag in items) {%>
  <div>## <%= tag %></div>

  <br />

  <% for (var i in items[tag]) {%>
    <div>- [<%= items[tag][i].title %>](<%= items[tag][i].url %>) </div>
  <% } %>

  <br />
<% } %>

</body>

</html>
